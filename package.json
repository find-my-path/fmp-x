{
  "name": "fmp-x",
  "version": "0.0.0",
  "scripts": {
    "start": "node index.js",
    "dev-api-serve": "env $(cat .env) nodemon server.js",
    "dev-web-serve": "cd web && npm start",
    "dev-api-test": "echo TODO: configure watch testing for server",
    "dev-web-test": "cd web && npm run dev-test",
    "test": "echo TODO: configure testing for server",
    "postinstall": "cd web && npm install",
    "posttest": "cd web && npm test"
  },
  "private": true,
  "dependencies": {
    "express": "^4.16.3",
    "mongoose": "^5.2.7",
    "morgan": "^1.9.0",
    "nodemon": "^1.18.3",
    "winston": "^3.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": "10.8.0"
  }
}
